# Лабораторная работа 4: Базовые растровые алгоритмы

## 📋 Описание проекта

Приложение для визуализации и изучения базовых растровых алгоритмов построения отрезков и окружностей на дискретной сетке. Реализованы четыре фундаментальных алгоритма компьютерной графики с подробной визуализацией каждого шага и измерением временных характеристик.

### Реализованные алгоритмы

1. **Пошаговый алгоритм (Step-by-Step Algorithm)**
   - Простейший алгоритм растеризации отрезка
   - Основан на линейной интерполяции координат
   - Использует округление вещественных координат

2. **Алгоритм ЦДА (DDA - Digital Differential Analyzer)**
   - Цифровой дифференциальный анализатор
   - Итеративное вычисление координат точек
   - Использует приращения для построения линии

3. **Алгоритм Брезенхема для отрезка (Bresenham's Line Algorithm)**
   - Целочисленный алгоритм без операций с плавающей точкой
   - Использует принцип накопления ошибки
   - Наиболее эффективный алгоритм для растеризации отрезков

4. **Алгоритм Брезенхема для окружности (Bresenham's Circle Algorithm)**
   - Построение окружности через восемь симметричных точек
   - Целочисленная арифметика
   - Оптимизированный алгоритм для растеризации окружностей

## 🎯 Функциональные возможности

### Основной функционал

- **Выбор алгоритма**: Переключение между четырьмя алгоритмами растеризации
- **Параметры построения**: 
  - Для отрезков: начальная точка (x1, y1) и конечная точка (x2, y2)
  - Для окружности: центр (xc, yc) и радиус (r)
- **Визуализация**: Отображение результата на дискретной сетке с координатами

### Визуализация

- **Координатная сетка**: 40×30 клеток с масштабируемым размером
- **Оси координат**: Четко обозначенные оси X и Y с подписями
- **Сетка с делениями**: Линии сетки с подписями каждые 5 единиц
- **Цветовая индикация**:
  - 🔵 Синий цвет — растеризованные точки
  - 🟢 Зеленый цвет — начальная точка отрезка
  - 🔴 Красный цвет — конечная точка отрезка
- **Дискретная сетка**: Наглядное отображение связи непрерывных координат с дискретными пикселями

### Информационная панель

- **Параметры алгоритма**: Отображение выбранного алгоритма и входных данных
- **Количество точек**: Число растеризованных точек
- **Время выполнения**: Измерение скорости работы алгоритма в микросекундах и миллисекундах
- **Пример вычислений**: Пошаговая демонстрация работы алгоритма с промежуточными значениями

### Детальные вычисления

Для каждого алгоритма отображаются:

**Пошаговый алгоритм и ЦДА:**
- Вычисление приращений dx, dy
- Определение количества шагов
- Значения x_increment и y_increment
- Первые 5 итераций с вещественными и округленными значениями

**Алгоритм Брезенхема (отрезок):**
- Вычисление dx и dy
- Начальное значение ошибки
- Пошаговое изменение координат и ошибки
- Условия выбора следующей точки

**Алгоритм Брезенхема (окружность):**
- Параметры центра и радиуса
- Начальное значение параметра решения
- Построение восьми симметричных точек
- Обновление параметра решения на каждом шаге

## 🚀 Установка и запуск

### Вариант 1: Скачать готовый .exe файл (для Windows)

#### Способ A: Скачать из Releases

1. Перейдите на страницу [Releases](https://github.com/YOUR_USERNAME/YOUR_REPOSITORY/releases) проекта
2. Скачайте последнюю версию файла `RasterAlgorithms.exe`
3. Запустите файл двойным кликом
4. ✅ Программа готова к использованию!

> **Примечание**: Windows может предупредить о неизвестном приложении. Нажмите "Подробнее" → "Выполнить в любом случае"

#### Способ B: Скачать весь репозиторий

1. На главной странице репозитория нажмите зеленую кнопку **Code**
2. Выберите **Download ZIP**
3. Распакуйте архив в любую папку
4. В папке `dist` найдите файл `RasterAlgorithms.exe`
5. Запустите файл двойным кликом

### Вариант 2: Запуск из исходного кода (все ОС)

#### Требования

- Python 3.7 или выше
- pip (обычно устанавливается вместе с Python)

#### Шаг 1: Установка Python

**Windows:**
1. Скачайте Python с [официального сайта](https://www.python.org/downloads/)
2. Запустите установщик
3. ⚠️ Обязательно отметьте галочку **"Add Python to PATH"**
4. Нажмите "Install Now"

**Linux/MacOS:**
```bash
# Обычно Python уже установлен. Проверьте версию:
python3 --version

# Если Python не установлен:
# Ubuntu/Debian:
sudo apt update
sudo apt install python3 python3-pip python3-tk

# MacOS (через Homebrew):
brew install python-tk
```

#### Шаг 2: Клонирование репозитория

```bash
# Клонируйте репозиторий
git clone https://github.com/YOUR_USERNAME/YOUR_REPOSITORY.git

# Перейдите в папку проекта
cd YOUR_REPOSITORY
```

Или скачайте ZIP-архив:
1. Нажмите зеленую кнопку **Code** → **Download ZIP**
2. Распакуйте архив
3. Откройте папку в терминале/командной строке

#### Шаг 3: Установка зависимостей (опционально)

```bash
# Установка зависимостей (для создания .exe)
pip install -r requirements.txt
```

> **Примечание**: Для запуска программы дополнительные зависимости НЕ требуются! 
> Все используемые модули (tkinter, time, math, typing) входят в стандартную библиотеку Python.

#### Шаг 4: Запуск программы

```bash
python main.py
```

или

```bash
python3 main.py
```

### Вариант 3: Создание собственного .exe файла (для Windows)

Если вы хотите самостоятельно создать исполняемый файл:

```bash
# 1. Установите PyInstaller
pip install pyinstaller

# 2. Создайте .exe файл
pyinstaller --onefile --windowed --name=RasterAlgorithms main.py

# 3. Готовый файл появится в папке dist/
# dist/RasterAlgorithms.exe
```

Параметры PyInstaller:
- `--onefile` — создает один исполняемый файл
- `--windowed` — скрывает консоль (для GUI приложений)
- `--name` — имя выходного файла

## 📖 Инструкция по использованию

### Построение отрезка

1. **Выберите алгоритм**:
   - Пошаговый алгоритм
   - Алгоритм ЦДА
   - Алгоритм Брезенхема (отрезок)

2. **Задайте параметры**:
   - Начальная точка (x1, y1)
   - Конечная точка (x2, y2)
   - Пример: от (5, 5) до (15, 12)

3. **Нажмите кнопку "Построить"**

4. **Изучите результаты**:
   - Визуализация на сетке
   - Количество точек
   - Время выполнения
   - Пошаговые вычисления

### Построение окружности

1. **Выберите алгоритм**:
   - Алгоритм Брезенхема (окружность)

2. **Задайте параметры**:
   - Центр окружности (xc, yc)
   - Радиус (r)
   - Пример: центр (20, 15), радиус 8

3. **Нажмите кнопку "Построить"**

4. **Изучите результаты**:
   - Окружность из дискретных точек
   - Информация о построении

### Сравнение алгоритмов

Для сравнения временных характеристик:

1. Выберите одинаковые параметры отрезка
2. Постройте отрезок каждым из трех алгоритмов
3. Сравните время выполнения в информационной панели

**Ожидаемые результаты**:
- Алгоритм Брезенхема — самый быстрый (целочисленная арифметика)
- ЦДА — средняя скорость
- Пошаговый — может быть медленнее (операции с плавающей точкой)

## 🔍 Техническая документация

### Архитектура проекта

Проект состоит из одного модуля `main.py` с двумя основными классами:

#### Класс `RasterAlgorithms`

Содержит статические методы для всех алгоритмов растеризации:

```python
class RasterAlgorithms:
    @staticmethod
    def step_by_step(x1, y1, x2, y2) -> List[Tuple[int, int]]
    
    @staticmethod
    def dda(x1, y1, x2, y2) -> List[Tuple[int, int]]
    
    @staticmethod
    def bresenham_line(x1, y1, x2, y2) -> List[Tuple[int, int]]
    
    @staticmethod
    def bresenham_circle(xc, yc, r) -> List[Tuple[int, int]]
```

Каждый метод возвращает список координат растеризованных точек.

#### Класс `RasterVisualizerApp`

Главный класс приложения, управляющий GUI:

- `setup_ui()` — создание интерфейса
- `draw_grid()` — отрисовка координатной сетки
- `draw_point()` — отрисовка точки на сетке
- `build_raster()` — выполнение алгоритма и визуализация
- `on_algorithm_change()` — переключение между параметрами

### Используемые технологии

- **Python 3.7+**: Основной язык программирования
- **tkinter**: Стандартная библиотека для создания GUI
- **time.perf_counter()**: Высокоточное измерение времени
- **typing**: Типизация для улучшения читаемости кода

### Параметры визуализации

```python
self.cell_size = 20      # Размер клетки в пикселях
self.grid_width = 40     # Ширина сетки в клетках
self.grid_height = 30    # Высота сетки в клетках
self.offset_x = 50       # Отступ слева для подписей
self.offset_y = 50       # Отступ сверху для подписей
```

Эти параметры можно изменить для настройки размера сетки.

## 📊 Примеры работы

### Пример 1: Пошаговый алгоритм

**Параметры**: от (5, 5) до (15, 12)

**Вычисления**:
```
dx = 15 - 5 = 10
dy = 12 - 5 = 7
steps = max(|10|, |7|) = 10
x_increment = 10/10 = 1.0000
y_increment = 7/10 = 0.7000

Шаг 0: x=5.00, y=5.00 → (5, 5)
Шаг 1: x=6.00, y=5.70 → (6, 6)
Шаг 2: x=7.00, y=6.40 → (7, 6)
Шаг 3: x=8.00, y=7.10 → (8, 7)
Шаг 4: x=9.00, y=7.80 → (9, 8)
...
```

**Результат**: 11 точек, время ~50-100 мкс

### Пример 2: Алгоритм ЦДА

**Параметры**: от (5, 5) до (15, 12)

**Отличия от пошагового**:
- Использует округление `int(x + 0.5)` вместо `round(x)`
- Результат идентичен для большинства случаев
- Время выполнения схоже

### Пример 3: Алгоритм Брезенхема (отрезок)

**Параметры**: от (5, 5) до (15, 12)

**Вычисления**:
```
dx = |15 - 5| = 10
dy = |12 - 5| = 7
Начальная ошибка: err = dx - dy = 10 - 7 = 3

Шаг 0: (5, 5), err=3
Шаг 1: (6, 6), err=0
Шаг 2: (7, 6), err=3
Шаг 3: (8, 7), err=0
...
```

**Результат**: 11 точек, время ~30-60 мкс (быстрее!)

### Пример 4: Алгоритм Брезенхема (окружность)

**Параметры**: центр (20, 15), радиус 8

**Вычисления**:
```
Центр: (20, 15)
Радиус: 8
Начальное значение: d = 3 - 2*8 = -13

Шаг 0: x=0, y=8, d=-13
  → 8 точек: (20±0, 15±8), (20±8, 15±0)
Шаг 1: x=1, y=8, d=-9
  → 8 точек: (20±1, 15±8), (20±8, 15±1)
...
```

**Результат**: ~88 точек (без дубликатов), время ~80-150 мкс

## 🎓 Теоретические основы

### Пошаговый алгоритм

**Идея**: Линейная интерполяция между двумя точками.

**Формула**:
```
x(i) = x₁ + i * (x₂ - x₁) / steps
y(i) = y₁ + i * (y₂ - y₁) / steps
```

**Преимущества**:
- Простота реализации
- Наглядность

**Недостатки**:
- Операции с плавающей точкой
- Накопление ошибок округления

### Алгоритм ЦДА

**Идея**: Цифровой дифференциальный анализатор, численное решение дифференциальных уравнений.

**Принцип**: Итеративное добавление приращений.

**Преимущества**:
- Простая реализация
- Хорошая точность

**Недостатки**:
- Операции с плавающей точкой
- Медленнее целочисленных алгоритмов

### Алгоритм Брезенхема (отрезок)

**Идея**: Целочисленная арифметика с накоплением ошибки.

**Принцип**: 
- На каждом шаге выбирается ближайший пиксель
- Решение основано на анализе знака ошибки

**Преимущества**:
- Только целочисленные операции
- Высокая скорость
- Не накапливает ошибки округления

**Недостатки**:
- Более сложная реализация

### Алгоритм Брезенхема (окружность)

**Идея**: Использование восьмикратной симметрии окружности.

**Принцип**:
- Вычисляется одна восьмая окружности (45°)
- Остальные точки получаются отражением

**Преимущества**:
- Целочисленная арифметика
- Высокая эффективность
- Использование симметрии

## 🎯 Критерии оценки (100 баллов)

### Корректность работы методов (60 баллов)

✅ **Пошаговый алгоритм** (10 баллов):
- Реализован метод `step_by_step()`
- Выводятся сопроводительные вычисления
- Показаны промежуточные значения

✅ **Алгоритм ЦДА** (10 баллов):
- Реализован метод `dda()`
- Выводятся формулы и вычисления
- Демонстрация первых итераций

✅ **Алгоритм Брезенхема (отрезок)** (20 баллов):
- Реализован метод `bresenham_line()`
- Показано вычисление ошибки
- Пошаговая демонстрация алгоритма

✅ **Алгоритм Брезенхема (окружность)** (20 баллов):
- Реализован метод `bresenham_circle()`
- Использование восьмикратной симметрии
- Демонстрация параметра решения

### Временные характеристики (20 баллов)

✅ **Измерение времени выполнения**:
- Использован `time.perf_counter()` для точности
- Время в микросекундах и миллисекундах
- Сравнение алгоритмов возможно

✅ **Краткий отчет**:
- Информация выводится в отдельной панели
- Параметры алгоритма
- Количество точек и время

### Дружелюбный интерфейс (20 баллов)

✅ **Масштаб**:
- Настраиваемый размер клеток (20 пикселей)
- Видимая область 40×30 клеток

✅ **Координаты**:
- Подписи по осям X и Y
- Деления каждые 5 единиц

✅ **Оси**:
- Четкие оси координат
- Подписи осей

✅ **Линии сетки**:
- Сетка для всей области
- Контрастные цвета

✅ **Подписи**:
- Числовые значения координат
- Легенда с цветами

✅ **Объяснение дискретной сетки**:
- Наглядная связь координат с пикселями
- Цветовая индикация различных элементов

## 🔧 Устранение неполадок

### Windows: "Python не распознается как команда"

**Решение**:
1. Переустановите Python с галочкой "Add Python to PATH"
2. Или запустите через Python Launcher: `py main.py`

### Linux/MacOS: "ModuleNotFoundError: No module named '_tkinter'"

**Решение Ubuntu/Debian**:
```bash
sudo apt-get install python3-tk
```

**Решение MacOS**:
```bash
brew install python-tk
```